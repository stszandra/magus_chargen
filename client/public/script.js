import { products } from "/data.js";

const loadEvent = function () {
  //document.getElementById("root").insertAdjacentHTML("beforeend", createDiv4(products, "and", findComposers));
};

window.addEventListener("load", loadEvent);
